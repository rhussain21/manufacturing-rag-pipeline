[theme]
# Force light base — let CSS handle all visual theming, not Streamlit's theme vars
base = "light"
primaryColor = "#CC6A47"
textColor = "#2D2A26"
font = "sans serif"

[browser]
gatherUsageStats = false
