"""
Agents package — being rebuilt on LangGraph.

The old hand-rolled RouterAgent/SQLAgent/VectorAgent/WebAgent + BaseAgent
framework has been removed. Next up: a Technical Doc Expert node (retrieval
over the manufacturing corpus), then a LangGraph StateGraph wired around it.
"""
